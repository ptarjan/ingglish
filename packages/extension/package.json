{
  "name": "@ingglish/extension",
  "version": "1.0.0",
  "description": "Browser extension to translate any webpage to phonetic Ingglish",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/content-lite.ts src/background.ts src/popup.ts --format iife --outDir dist --clean",
    "postbuild": "node scripts/copy-assets.js && node scripts/generate-icons.js",
    "build:icons": "node scripts/generate-icons.js",
    "lint": "eslint --cache src",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@ingglish/core": "^1.0.0",
    "@ingglish/dom": "^1.0.0",
    "@ingglish/normalize": "^1.0.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "@vitest/coverage-v8": "^4.0.16",
    "jsdom": "^28.1.0",
    "sharp": "^0.34.5",
    "tsup": "^8.4.0",
    "typescript": "^5.8.3",
    "vitest": "^4.0.16"
  }
}
