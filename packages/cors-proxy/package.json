{
  "name": "@ingglish/cors-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker CORS proxy for URL translation",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint --cache src",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run --no-color"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "@vitest/coverage-v8": "^4.0.16",
    "typescript": "^5.8.3",
    "vitest": "^4.0.16",
    "wrangler": "^4.54.0"
  }
}
